The file "data.json" in folder "HusqvarnaWpfApp" will be copied to
the output directory (HusqvaranaWpfApp/bin/Debug/net7.0-windows) at build time.
The program will monitor the file in the output directory.
